import {StyleSheet} from 'react-native';
import React from 'react';

export const styles = StyleSheet.create({
  headerContainer: {
    backgroundColor: 'red',
    padding: 12,
    marginBottom:15

  },
  headTitle: {
    fontSize: 20,
    fontWeight: '600',
    color: '#fff',
  },
  card: {
    margin: 10,
    elevation: 2,
  },
});
